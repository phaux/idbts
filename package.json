{
  "workspaces": [
    "idbts",
    "idbts-react"
  ],
  "scripts": {
    "prepare": "npm run prepare --ws",
    "check": "npm run check --ws",
    "test": "npm test --ws"
  },
  "prettier": {
    "printWidth": 100
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  }
}
